apiVersion: v1
kind: Service
metadata:
  name: {{ include "w7panel-kubeblocks.fullname" . }}
  labels:
    {{- include "w7panel-kubeblocks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "w7panel-kubeblocks.selectorLabels" . | nindent 4 }}
